{
  "model_name": "thenlper/gte-large",
  "device": "cuda:0",
  "tokenizer": {
    "max_length": 512,
    "padding": true,
    "truncation": true
  },
  "epochs": 5,
  "pool": "mean",
  "reset": false,
  "scheduler": "warmuplinear",
  "warmup_steps": 500,
  "optimizer": "AdamW",
  "weight_decay": 0.1,
  "learning_rate": 1e-5,
  "batch_size": 8,
  "id": "TP2",
  "dataset": "only_report"
}